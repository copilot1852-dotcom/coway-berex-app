# Notion 스타일 디자인 변환 지시어

아래 3개 파일을 참고하여 이 앱의 디자인을 Notion 스타일로 변환해줘.

## 첨부 파일
1. `DESIGN_GUIDE.md` — 색상, 타이포, 컴포넌트 규칙 (이 파일이 최우선 기준)
2. `mockup-main.html` — 메인 화면 시안 (브라우저로 열어서 참고)
3. `mockup-notion.html` — 슬라이드 화면 시안 (브라우저로 열어서 참고)

## 핵심 규칙 요약

### 색상
- 브랜드 컬러: `#2eaadc` (Notion 블루)
- 할인/절약: `#eb5757` (Notion 레드)
- 본문: `#37352f` / 보조: `#787774` / 라벨: `#9b9a97`
- 배경: `#FFFFFF` / 카드: `#f7f6f3` / 테두리: `#e9e9e7`
- 다크모드: 배경 `#191919` / 카드 `#202020` / 테두리 `#2f2f2f`

### 형태
- border-radius: **4px** (모든 카드, 버튼, 칩, 입력)
- box-shadow: **없음** (그림자 전면 제거)
- 구분선: **1px solid #e9e9e7** (선으로만 구분)
- pill 형태(border-radius 100px) **금지**
- 라운드 카드(border-radius 10px 이상) **금지**

### 타이포그래피
- 폰트: Pretendard(한글) + Inter(숫자)
- 정보 위계는 **크기와 굵기**로만 표현 (색상/장식 아님)
- HERO 숫자: 42px, weight 200
- 제목: 17px, weight 700~800
- 본문: 13~14px, weight 400~500
- 라벨: 11~12px, weight 500

### 원칙
1. 장식 최소화 — 정보 전달에 불필요한 요소 제거
2. 선과 여백으로 구분 — 카드/그림자 대신
3. 시니어 가독성 — 대비율 충분히, 핵심 숫자 크게
4. CSS 변수 사용 — 하드코딩 색상 금지

## 작업 방식
1. CSS 변수(색상)를 먼저 일괄 변경
2. border-radius를 4px로 일괄 변경
3. box-shadow를 none으로 일괄 변경
4. inline style의 하드코딩 색상 제거
5. 계산 로직/레이아웃 구조는 건드리지 않음
6. 변경 전 백업 필수
