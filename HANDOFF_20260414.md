# 세션 핸드오프 — 2026.04.14

> 이 문서를 새 세션 첫 메시지에 붙여넣으세요.

---

## 1. 프로젝트 개요

- **앱**: 비렉스(BEREX) 공식 가격 가이드 v5
- **목적**: 코웨이 매트리스 렌탈 영업사원용 견적·비교·안내 앱
- **스택**: 단일 HTML (React 18 + Babel 인라인), PWA
- **경로**: `/Users/minmacbook/Desktop/V5 festa/index.html`
- **빌드**: `bash build.sh` → JSX→JS 컴파일 → `deploy/` 폴더
- **프리뷰**: `bash preview.sh` → 빌드+복사 자동화 (hook 설정됨)
- **서버**: WEBrick → `/tmp/festa-deploy/` 서빙 (포트 8080/8081)

---

## 2. 핵심 파일 구조

```
/Users/minmacbook/Desktop/V5 festa/
├── index.html          (943KB) ← 소스 (수정 대상)
├── build.sh            ← JSX→JS 빌드
├── preview.sh          ← 빌드+deploy복사 자동화 (신규)
├── deploy/
│   ├── index.html      (931KB) ← 빌드 결과물
│   ├── sw.js           ← Service Worker
│   ├── manifest.json
│   └── data/           ← competitorMap.json, catalog/healing.json
├── .claude/
│   ├── launch.json     ← 프리뷰 서버 설정 (festa:8080, mockup:8081)
│   └── settings.json   ← Edit/Write 후 자동 preview.sh 실행 hook
├── CALC_LOGIC.md       ← 할인 계산 공식 명세
├── STRUCTURE.md        ← 코드 구조 문서
├── DESIGN_GUIDE.md     ← UI 디자인 가이드
├── DEV_RULES.md        ← 개발 규칙
├── 비렉스_가격표_202604.html  ← A4 인쇄용 가격표 (신규)
├── 비렉스_가격표_202604.docx  ← Word 가격표 (신규)
└── 비렉스_5년_서프_세트비교표.docx ← 세트 비교표 (신규)
```

---

## 3. 이번 세션 완료 작업

### UI 변경
| # | 작업 | 상태 |
|---|---|---|
| 1 | 페스타 15% 토글 제거 → **결합 5% 한 줄** 토글로 변경 | ✅ |
| 2 | **반값 상세 카드** 추가 (메인: 처음 N개월/이후 금액 표시) | ✅ |
| 3 | **혜택보기 명세서** 반값 기간별 상세 (처음/이후 sub-line) | ✅ |
| 4 | **페스타 D-day 배지** (가격 옆 + 반값 카드 헤더) | ✅ |
| 5 | **최저 약정 표시** (배지 "최저" + 힌트 "N년이 X원 더 저렴") | ✅ |
| 6 | 카드 공지 팝업 **4월말 연장** (4/7 → 5/1) | ✅ |

### 계산 로직 변경
| # | 작업 | 상태 |
|---|---|---|
| 7 | **페스타 D-day 자동 전환** (IS_FESTA: 4/28 기준) | ✅ |
| 8 | 패키지 할인 **15% → 10%** 자동 전환 (IS_FESTA=false 시) | ✅ |
| 9 | 반값 할인 **자동 비활성** (IS_FESTA=false 시 festaMonths=0) | ✅ |
| 10 | 할인 라벨 자동 전환 ("페스타 15%" → "패키지 10%") | ✅ |
| 11 | **절약 계산식 통일**: 정상가 합계 - 총납입 (혜택보기와 동일) | ✅ |
| 12 | **등록비 면제**: 고정 10만 → **대수 × 10만원** | ✅ |
| 13 | 메인 4분할 절약에 **등록비 포함** | ✅ |

### 성능 최적화
| # | 작업 | 상태 |
|---|---|---|
| 14 | 최저 약정 계산 **useMemo 캐싱** (bestPeriodInfo) | ✅ |
| 15 | **preview.sh** 자동화 + hook 설정 | ✅ |

### 문서/출력물
| # | 작업 | 상태 |
|---|---|---|
| 16 | 5년 서프 세트 비교표 DOCX (6그룹 18조합) | ✅ |
| 17 | **A4 인쇄용 가격표** HTML + DOCX | ✅ |
| 18 | 아이패드 미니6 **오프라인 빌드** (index-offline.html) | ✅ |

---

## 4. 핵심 계산 로직 변경 상세

### IS_FESTA 자동 전환 (Line ~1107-1108)
```javascript
const FESTA_END_DATE = new Date('2026-04-28T00:00:00');
const IS_FESTA = new Date() < FESTA_END_DATE;
```
- IS_FESTA=true: 15% 패키지, 반값 최대 15개월
- IS_FESTA=false (4/28 이후): **10% 패키지, 반값 0**

### discountRate (Line ~4390)
```javascript
const discountRate = isRerental ? 0 : (totalUnitsCount >= 2 ? (IS_FESTA ? 0.15 : 0.10) : (isCross ? 0.05 : 0));
```

### festaMonths (Line ~4447)
```javascript
if (!isRerental && IS_FESTA) { // ← IS_FESTA 조건 추가
  if (totalUnitsCount === 1) festaMonths = PROMOTION_FREE_MONTHS[selectedPeriod];
  // ...
}
```

### 절약 계산 (Line ~5869-5871)
```javascript
const _rawTotal = (mBase*mQty + fBase*fQty) * contractMonths;  // 정상가
const _regFee = 100000 * (mQty + fQty + extras);               // 등록비 (대수×10만)
const _sav = _rawTotal - _ft + _regFee;                        // 절약 = 정상가 - 총납입 + 등록비
```

### 등록비 면제 (혜택보기, Line ~8205)
```javascript
const _regFee = 100000 * Math.max(1, cur.totalUnits || 1);  // 대수 × 10만
```

### bestPeriodInfo useMemo (Line ~4673)
```javascript
const bestPeriodInfo = useMemo(() => {
  // _computeHpResults로 5/7/9년 비교 → 최저 약정 판별
  // userTouched.mattress/frame 기준 수량 결정
}, [activeCare, activeMattress, ...]);
```

---

## 5. 수정된 파일 목록

| 파일 | 변경 내용 |
|---|---|
| `index.html` | 전체 UI/로직 변경 (위 3번 항목 전체) |
| `preview.sh` | 신규 — 빌드+복사 자동화 스크립트 |
| `.claude/settings.json` | 신규 — Edit/Write 후 자동 preview.sh hook |
| `.claude/CLAUDE.md` | `?` 구조도 프로토콜 추가 |
| `비렉스_가격표_202604.html` | 신규 — A4 인쇄용 가격표 |
| `비렉스_가격표_202604.docx` | 신규 — Word 가격표 |
| `비렉스_5년_서프_세트비교표.docx` | 신규 — 세트 비교표 |

---

## 6. 미완성 / 추가 확인 필요

| 항목 | 상태 | 비고 |
|---|---|---|
| 모드 탭 "페스타" → "신규" 라벨 전환 | ⚠️ 미구현 | IS_FESTA=false 시 탭 이름 변경 필요 |
| D-day 배지 D-0 이하 자동 숨김 | ⚠️ 미구현 | 현재 D-0도 표시됨 |
| 고객 대면 모드 (목업만 완료) | 💡 기획 완료 | 구현 필요 시 목업 참조 |
| 스텝 가이드 (목업만 완료) | 💡 기획 완료 | 구현 필요 시 목업 참조 |
| 견적서 문자 템플릿 고도화 (목업만 완료) | 💡 기획 완료 | 반값/최저약정/카드 추천 포함 |
| 아이패드 오프라인 빌드 동기화 | ⚠️ | 앱 변경 시 index-offline.html 재생성 필요 |

---

## 7. 다음 세션 추천 작업

1. **모드 탭 라벨 전환** — "페스타" → IS_FESTA ? "페스타" : "신규"
2. **D-day 배지 숨김** — D-0 이하면 자동 숨김
3. **고객 대면 모드 구현** — 영업 현장 최적화 (목업 완료)
4. **견적서 템플릿 고도화** — 반값/최저약정 정보 포함
5. **CALC_LOGIC.md 업데이트** — IS_FESTA, 등록비 대수 반영, 절약 공식 변경

---

## 8. 주의사항

### 프리뷰 반영 4단계 (반드시 순서대로)
```
1. index.html 수정
2. bash preview.sh (또는 hook이 자동 실행)
3. 프리뷰 서버 캐시 → 서버 재시작 or 새 포트 사용
4. 브라우저 캐시 → ?v=timestamp 추가 or SW 해제
```
- `preview.sh`가 2단계까지 자동 처리
- 3~4단계 문제 시 `mockup`(8081) 포트로 새 서버 시작

### 계산 연동 구조
```
PRODUCT_DATA (원본 가격)
  → getDetailedPricingByCare() (할인 계산 엔진)
    → calculatedPrices (useMemo, 메인 가격)
    → _computeHpResults() (5/7/9년 비교)
      → bestPeriodInfo (useMemo, 최저 약정)
    → calculateCartPricing() (장바구니 합산)
```

### 절약 계산 주의
- **메인 4분할**: 정상가(mBase*months) - 총납입 + 등록비면제
- **혜택보기**: 정상가(donutTotalRaw) - 총납입 + 등록비면제
- **매월 절약**: 등록비 미포함 (일시성이라)
- 세 곳의 절약이 **서로 다른 기준**일 수 있음 — 수정 시 전부 확인

### 숫자 데이터 규칙
- 가격/스펙 숫자는 **절대 임의 생성 금지** → 사용자 확인 후 코딩
- `?` 프로토콜: 요청 끝에 `?` 붙이면 구조도 먼저 보여주고 승인 후 실행

---

## 9. 구조도 프로토콜 (`?`)

사용자가 요청 끝에 `?`를 붙이면:
1. 실행하지 않음
2. 이해한 구조도 보여줌 (요청 본질 / 영향 범위 / 확인 필요 사항)
3. 승인 후 실행

CLAUDE.md에 등록 완료.
