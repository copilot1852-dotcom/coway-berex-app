# 세션 35 핸드오프 문서

## 1. 프로젝트 개요
- **앱 이름**: 비렉스 공식 가격 가이드 v5 (코웨이 매트리스 렌탈 견적 앱)
- **기술 스택**: 단일 HTML 파일 (React 18 + Babel 인라인), PWA
- **목적**: 코웨이 홈케어닥터가 고객 집에서 태블릿으로 보여주며 렌탈 계약을 설득하는 현장 영업 도구
- **경로**: `/Users/minmacbook/Desktop/V5 festa/index.html`
- **빌드**: `sh build.sh` → `deploy/` 폴더 (JSX→JS 사전 변환, Babel standalone 제거)
- **배포**: deploy/ → Cloudflare Pages

## 2. 파일 구조
```
/Users/minmacbook/Desktop/V5 festa/
├── index.html          ← 메인 소스 (930KB+)
├── index1.html         ← 세션 35 시작 시 백업
├── build.sh            ← 빌드 스크립트
├── deploy/             ← 빌드 결과물 (배포용)
│   └── index.html      ← build.sh로 생성 (cp 금지!)
├── data/
│   └── competitorMap.json
├── CALC_LOGIC.md       ← 계산 로직 명세서 ★ 필독
├── DEV_RULES.md        ← 개발 규칙 ★ 필독
├── DESIGN_GUIDE.md     ← 디자인 가이드
├── STRUCTURE.md        ← 앱 구조도
├── PROGRESS.md         ← 작업 히스토리
└── HANDOFF_SESSION35.md ← 이 문서
```

## 3. 세션 35 완료된 작업

### 3-1. mLE = 5000 루네어 특별할인 추가 (계산 엔진)
- `index.html:4242` — `if (selectedMattress === 'lunaire') mLE = 5000;`
- 원래 코드에 mLE 구조만 있고 값 할당이 누락됨 → 추가
- mFinal에 영향 없음 (mBase에 +, prePkg에서 - 상쇄, 표시용)
- 코웨이 공식앱 정상가(취소선)와 일치 확인됨

### 3-2. 도넛 슬라이드1 — 비용 명세서 통합 카드
- 세그먼트 범례 + 할인 상세를 하나의 카드로 합침
- 정상가 합계 행 추가
- donutTotal = totalRentPayable (실제 최종 납입액)
- 카드 할인 상세(기간별/연회비) 복원

### 3-3. 도넛 세그먼트 — 할인 전 기준으로 변경
- 매트리스: (mBase - aFee) × months - careRawTotal (탑퍼/방문관리 분리)
- 프레임: fBase × months
- 탑퍼: aFee × months = 할인 전 원가 (사용자 요청)
- 방문관리: 6,000 × months = 할인 전 원가 (사용자 요청)
- 추가 제품: prePkg × qty × months (할인 전)

### 3-4. 슬라이드2 — 정상렌탈료 3단 비교 바
- 일시불 / 정상 렌탈료(rawVal) / 할인 후(rVal) 3줄 비교
- 일시불 대비 %, 정상가 대비 % 분리 표시

### 3-5. MD 파일 대규모 업데이트
- CALC_LOGIC.md: §14 도넛 표시 로직, §15 카탈로그 원가 대조 규칙, 도넛 할인 표시 전략
- DEV_RULES.md: ★★★ 가격/할인 항목 임의 생성 금지, ★★★ 코딩 전 손계산 검증 필수

## 4. 계산 로직 검증 결과

### 계산 엔진 = 완벽 (코웨이 공식앱과 100% 일치)
```
카탈로그 PDF = 토탈케어 + 자동이체 적용 후 = mFinal(토탈, 1대)
공식앱 정상가(취소선) = mBase
공식앱 7개월차~ = prePkg
공식앱 반값 = prePkg × 0.5

검증 (하이브리드4 퀸 5년):
  카탈로그 = 46,900원
  코드: mBase(토탈) = 47,900, mFinal = 47,900 - 1,000 = 46,900 ✓
```

### 코웨이 공식앱 할인 구조 (모디 슈싱 5년 기준, 4케어 검증 완료)
| 케어 | 정상가(mBase) | 7개월차~(prePkg) | 할인 항목 |
|------|-------------|----------------|---------|
| 서프 | 28,900 | 23,900 | 자동이체 + 매월4,000(mBC) + 반값 |
| 스페셜 | 32,900 | 27,900 | 자동이체 + 매월4,000(mBC) + 반값 |
| 베이직 | 30,900 | 29,900 | 자동이체 + 반값 |
| 토탈 | 34,900 | 33,900 | 자동이체 + 반값 |

## 5. 수정된 파일 목록

| 파일 | 변경 내용 |
|------|---------|
| index.html | mLE=5000 추가, 도넛 세그먼트 mBase 기준 변경, 비용 명세서 통합, 방문관리 360,000/탑퍼 240,000 분리, 슬라이드2 3단 비교, 방문관리 페스타 할인(임의생성 ← 삭제 필요) |
| CALC_LOGIC.md | §14 도넛 표시, §15 카탈로그 원가, 특수 모델 mLE, 할인 표시 전략 |
| DEV_RULES.md | ★★★ 임의 생성 금지, ★★★ 손계산 검증 필수, 도넛 정합성 섹션 업데이트 |
| deploy/index.html | build.sh로 빌드됨 |

## 6. 미완성 / 삭제 필요

### ★ "방문관리 페스타 할인" 삭제 (최우선)
- 임의 생성한 할인 항목. 코웨이 공식앱에 없음
- 삭제 대상 코드:
  - `_careRawM = 6000` 변수
  - `_dCareDiscM`, `_dCareDiscTotal` 변수
  - `careDiscM`, `careDiscTotal` step push 필드
  - 할인 상세 렌더링에서 `cur.careDiscTotal > 0` 블록
  - 총 할인 합산에서 `(cur.careDiscTotal||0)` 제거

### 정합성 확인 필요
- 방문관리 페스타 할인 삭제 후 세그먼트합 - 할인합 = donutTotal 검증
- 방문관리 360,000(할인전)과 탑퍼 240,000(할인전)이 세그먼트에 있는데 대응 할인이 없으면 정합 불일치 가능
- **임의 해결 금지 → 사용자에게 보고 후 방향 결정**

## 7. 다음 세션에서 이어서 할 작업

1. "방문관리 페스타 할인" 삭제
2. 삭제 후 정합성 검증 → 불일치 시 보고
3. build.sh 실행 및 deploy 검증
4. smarts8 mSE=9000 도넛 표시 이슈 (보류, 사용자가 "다음에 해결하자"고 함)

## 8. 주의사항

### 절대 금지
- 가격/할인 항목 임의 생성 (DEV_RULES.md ★★★ 최상단 참조)
- 코딩 전 손계산 없이 작업 시작
- deploy에 cp로 복사 (반드시 build.sh 실행)
- 계산 엔진 함수 임의 수정

### 필수 절차
- 작업 시작 전 DEV_RULES.md + CALC_LOGIC.md + STRUCTURE.md + DESIGN_GUIDE.md 4개 파일 읽기
- 가격 관련 변경 시: 손계산 → 시뮬레이션 보고 → 승인 → 코딩
- 기준 케이스 검증: 퀸/5년/루네어/코지/토탈 → 59,320원

### 카탈로그 원가 대조
```
카탈로그 PDF 위치: /Users/minmacbook/Desktop/앱견적서.26.4/카탈로그.pdf
카탈로그 = 토탈케어 + 자동이체 후 = mFinal(토탈)
역산: mBase(토탈) = 카탈로그 + 1,000
```

### 도넛 할인 표시 전략
- 할인을 최대한 쪼개서 영업 임팩트 극대화
- 쪼개기 OK (mBC → 현장/VIP), 만들기 금지 (없는 항목 생성)

## 9. 새 세션 시작 메시지

```
HANDOFF_SESSION35.md 읽고 남은 작업부터 진행해줘.
최우선: "방문관리 페스타 할인" 임의 생성 항목 삭제.
삭제 후 정합성 검증하고 보고해줘. 임의 해결 금지.
```
