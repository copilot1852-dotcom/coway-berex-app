# 세션 핸드오프 — 재렌탈/페스타 계산 구조 개편

> 세션 날짜: 2026-04-04
> 상태: 설계 확정, 구현 대기

---

## 배경

현재 앱은 `[페스타] [재렌탈]` 모드를 전체 전환하는 구조.
실제로는 **제품마다 재렌탈/페스타가 다를 수 있음**.

예: 정수기 1대 재렌탈 + 침대 신규 렌탈 → 정수기만 재렌탈 가격, 침대는 페스타 가격

---

## 핵심 요구사항

### 1. 앱 시작 시 설문 추가

```
Q1. 재렌탈 제품이 몇 대인가요? → 0, 1, 2, 3...
Q2. 4월에 렌탈한 제품이 몇 대인가요? → 0, 1, 2, 3...
```

### 2. 재렌탈 수량 → 장바구니 순서대로 적용

```
재렌탈 N개 입력 시:
  장바구니 1번째~N번째 제품 → 재렌탈 가격
  N+1번째 이후 제품 → 페스타 가격

예시:
  재렌탈 1개, 매트+프레임 견적:
    매트(1번째) → 재렌탈 가격
    프레임(2번째) → 페스타 가격 (1대 기준)

  재렌탈 2개, 매트+프레임 견적:
    매트(1번째) → 재렌탈 가격
    프레임(2번째) → 재렌탈 가격

  재렌탈 0개:
    전부 페스타 가격 (현재와 동일)
```

### 3. 4월 렌탈 수량 → totalUnitsCount에 합산

```
4월 렌탈 N개 입력 시:
  기존 N대가 totalUnitsCount에 포함
  → 패키지 15% 할인율 결정에 영향
  → 반값 개월수 결정에 영향

예시:
  4월 렌탈 1개 + 신규 매트+프레임 = 3대 → 15% + 반값 3개월
  4월 렌탈 0개 + 신규 매트+프레임 = 2대 → 15% + 반값 0개월
```

---

## 변경 범위

### 제거/변경
- `[페스타] [재렌탈]` 상단 탭 → 제거 또는 의미 변경
- `promoMode` state ('festa' | 'rerental') → 전체 전환 방식 폐지
- `결합 5%` 토글 → 설문으로 대체 (기존 고객 = 재렌탈 or 4월 렌탈 1+)
- `페스타 15%` 토글 → 설문으로 대체 (4월 렌탈 1+)

### 추가
- 설문 UI (담당자 등록 후 또는 메인 화면 상단)
- `reRentalCount` state: 재렌탈 제품 수량
- `aprilRentalCount` state: 4월 렌탈 제품 수량
- 장바구니 아이템별 `isReRental` 플래그 (순서 기반 자동 할당)

### 계산 엔진 변경 (⚠️ 고위험)
- `_getDetailedPricingByCareImpl` → 제품별로 festa/rerental 분기
- `calculateCartPricing` → 아이템마다 다른 promoMode 적용
- `totalUnitsCount` → aprilRentalCount 합산
- 재렌탈 가격 계산 로직은 기존 그대로 유지 (1~12개월 20%, 이후 restRate)

---

## 주의사항

1. **계산 엔진 변경은 반드시 손계산 시뮬레이션 후 승인 받기**
2. **기준 케이스 검증 필수**: 퀸/5년/루네어/코지/토탈 → 59,320원 (재렌탈0, 4월0일 때)
3. 재렌탈 계산기(isRerentalCalcOpen)와의 관계 정리 필요
4. 워터폴/도넛/명세서에서 제품별 재렌탈/페스타 구분 표시
5. 힐링 제품도 재렌탈 대상이 될 수 있음

---

## 현재 관련 코드 위치

| 항목 | 라인 | 비고 |
|------|------|------|
| promoMode state | ~3498 | 'festa' / 'rerental' |
| [페스타][재렌탈] 탭 | 헤더 영역 | 상단 세그먼트 |
| 결합5% 토글 | ~5733 | isCrossDiscount |
| 페스타15% 토글 | ~5744 | isAprilCombo |
| 재렌탈 계산 (엔진) | ~4247-4276 | first12m, restM, restRate |
| totalUnitsCount | ~4290 | mQty + fQty + extras + aprilCombo |
| calculateCartPricing | ~4500+ | 장바구니 합산 |

---

## 이번 세션에서 완료된 작업 (참고)

1. 힐링 제품 카탈로그 시스템 (healing.json + UI + 계산 연동)
2. 렌탈료 실적 포함 안내 (카드 표시되는 모든 곳에 추가)
3. 백업: index4.html (힐링 작업 전 원본)
